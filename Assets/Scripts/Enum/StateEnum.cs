using System;

public class StateEnum
{
    //Các Enum chứa các giá trị để gán cho Animation của từng object

    public enum EPlayerState {idle, run, jump, fall, doubleJump, wallSlide, wallJump, dash};
}
